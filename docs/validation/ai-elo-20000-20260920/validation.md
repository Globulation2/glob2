# Completed 20,000-game update

The adjacent build, rating-test, regression, interactive and profile logs record successful commands run by the completion script. The dataset asserts 20,000 unique games, 10,000 complete paired blocks, no map-seed overlap, and the planned second-batch generator quotas. The fresh profile PNG was visually inspected after capture: final ratings and labels fit, and the list retains ascending strength order. CI for this new commit must pass before merge. Do not merge automatically.

The broader macOS visual suite previously hit a map-preview zoom assertion outside these selector changes. No new simulation changes or cross-platform checksum equivalence are claimed.
