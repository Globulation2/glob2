# Fresh Maxima cohort validation

The adjacent logs record successful release build, six rating regression tests, independent optimizer comparison, custom-game regression, all three interactive launch flows and focused profile capture. The final fit uses 19,948 distinct successful games in 9,974 complete swapped-side pairs. The exclusion audit accounts for the remaining 52 scheduled games. Generator counts reflect retained data rather than the original quotas. The fresh screenshot still needs visual review; the new commit needs CI before merge. Never merge automatically.

No simulation code is changed by the rating publication; Maxima fixes are already on master. No fresh per-tick cross-platform checksum equivalence is claimed by this tournament. The broader macOS visual suite previously hit an unrelated map-preview zoom assertion.
